      <div class="piece">
      <div class="masterpiece">
      <img src="{{work._doc.image}}" height="360" width="500">
      <h3>{{work._doc.title}}</h3>   
      <p><b>{{work._doc.artist.name}}</b></p>
      <p>{{work._doc.description}}</p>
      <small>{{work._doc.date}}</small>
      <h3>Highest bid: € {{work._doc.bids}} by {{work._doc.buyer}}</h3>
      </div>
      <div class="bidform">
       <form action="/singlework?work_id={{work._id}}" method="post">
  <label for="">€</label>
  <input type="number" name="bid">
    <button type="submit">Bid</button>
</form>
     </div>
     </div>